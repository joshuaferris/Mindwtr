- Mobile project groups now match desktop, and the Manage section keeps its expanded state more reliably.
- Mobile editing is smoother with fenced Markdown code blocks, quicker task field toggles, and better share-capture keyboard dismissal.
- Calendar and sync flows are steadier with stronger due-date calendar push behavior, safer WebDAV handling, and better review/reminder coordination.
